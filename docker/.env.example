# ============================================
# Family Docker 部署环境变量模板
# 复制为 .env 后按需填写：cp .env.example .env
# ============================================

# 知识库根路径（宿主机实际路径，最终用户需改为自己的路径）
VAULTS_DIR=/home/lumin/Vaults
